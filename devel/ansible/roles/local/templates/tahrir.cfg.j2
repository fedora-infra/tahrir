# This file configuration values for development

SECRET_KEY = "development"
TEMPLATES_AUTO_RELOAD = True
SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_SECURE = False

SQLALCHEMY_DATABASE_URI = "sqlite:///{{ devel_dir }}/tahrir.db"
OIDC_CLIENT_SECRETS = "{{ devel_dir }}/oidc-api.json"

TAHRIR_OWNER_GROUPS = ["signed_fpca"]
TAHRIR_ADMIN_GROUPS = ["admins"]
TAHRIR_PNGS_PATH = "{{ devel_dir }}/fedora-badges/pngs"
TAHRIR_STLS_PATH = "{{ devel_dir }}/fedora-badges/stls"
TAHRIR_USE_FEDMSG = False

TAHRIR_EMAIL_DOMAIN = "tinystage.test"
TAHRIR_FAS_URL = "https://{{ auth_hostname }}/noggin"

# Cache
CACHE = {
    "backend": "dogpile.cache.dbm",
    "expiration_time": 10000,
    "arguments": {
        "filename": "{{ devel_dir }}/cachefile.dbm",
    },
}
